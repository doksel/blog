import { Link } from "react-router-dom";
import styled from "styled-components";

const FogotPassword = styled(Link)`
  cursor: pointer;
  font-family: "LatoLight", sans-serif;
  font-size: 16px;
  line-height: 23px;
  text-decoration-line: underline;
  color: #676974;
`;
export default FogotPassword;
